{
	"name": "@inlang/rpc",
	"type": "module",
	"private": true,
	"exports": {
		".": "./src/index.ts",
		"./router": "./src/router.ts"
	},
	"scripts": {
		"---- DEV AND BUILD": "Are not required. Direct TypeScript import is possible because this module is only used internally.",
		"test": "npm run build && tsc --noEmit && vitest run --passWithNoTests --coverage",
		"lint": "eslint ./src",
		"format": "prettier ./src --write",
		"clean": "rm -rf ./dist ./.turbo ./node_modules"
	},
	"dependencies": {
		"@inlang/env-variables": "*",
		"@inlang/sdk": "*",
		"@inlang/result": "*",
		"body-parser": "^1.20.2",
		"ts-dedent": "^2.2.0",
		"typed-rpc": "3.0.0"
	},
	"peerDependencies": {
		"express": "^4.18.2"
	},
	"license": "Apache-2.0",
	"version": "0.0.0",
	"devDependencies": {
		"vitest": "^0.34.1"
	}
}
