{
	"extends": "../../tsconfig.base.json",
	"include": ["src/**/*.ts", "lib/**/*.ts", "./env.ts", "./build.js"],
	"compilerOptions": {
		"lib": ["ESNext", "DOM"],
		"types": [],
		"outDir": "./dist",
		// must be set to lib for the build to work
		"rootDir": "./lib"
	}
}
