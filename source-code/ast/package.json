{
	"name": "@inlang/ast",
	"type": "module",
	"private": "true",
	"exports": {
		".": {
			"types": "./dist/index.d.ts"
		}
	},
	"files": [
		"./dist"
	],
	"scripts": {
		"build": "npm run clean && tsc --build",
		"dev": "tsc --watch",
		"test": "vitest run",
		"format": "prettier ./src/** -w --config ../../.prettierrc.json",
		"clean": "rm -r -f ./dist"
	},
	"dependencies": {},
	"devDependencies": {}
}
