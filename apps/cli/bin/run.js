#!/usr/bin/env node

// the comment above signals unix environments which interpreter
// to use when executing this file (node)

// importing the main file which automatically executes
// the function call within the file aka starts the cli
import '../dist/main.js';
