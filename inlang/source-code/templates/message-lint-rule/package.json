{
	"name": "@inlang/template-message-lint-rule",
	"version": "0.1.0",
	"type": "module",
	"private": "true",
	"files": [
		"./dist"
	],
	"scripts": {
		"build": "inlang plugin build --entry ./src/index.ts",
		"dev": "inlang plugin build --entry ./src/index.ts --watch",
		"test": "tsc --noEmit && vitest run --passWithNoTests"
	},
	"dependencies": {
		"@inlang/sdk": "workspace:*"
	},
	"devDependencies": {
		"@inlang/cli": "workspace:*",
		"typescript": "^5.5.2",
		"vitest": "^0.34.4"
	}
}
