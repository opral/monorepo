{
    "name": "@inlang/language-tag",
    "version": "0.0.1",
    "description": "This package defines a canonical fallback order for language tags.",
    "type": "module",
    "main": "dist/index.js",
    "types": "dist/index.d.ts",
    "scripts": {
        "build": "tsc",
        "dev": "tsc -w",
        "test": "vitest run --passWithNoTests",
        "clean": "rm -rf ./dist && rm -rf ./node_modules"
    },
    "files": [
        "dist"
    ],
    "devDependencies": {
        "@inlang/sdk": "workspace:*"
    }
}