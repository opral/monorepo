import { Inlang } from './inlang'

export { Inlang }

// TODO? wrapper around Inlang.translate that auto configures the project etc.?
/** export function t(...inlang.translate args){
 *      const inlang = new Inlang(deriveConfig())
 *      return inlang.translate(...args)
 * }
 */
