{
	"extends": "@inlang/source-code-configs/typescript/tsconfig.base.json",
	"include": ["src/**/*"],
	"exclude": ["**/*.test.*", "**/*.stories.ts"],
	"compilerOptions": {
		"lib": ["ESNext", "DOM"],
		"outDir": "dist",
		// lit makes heavy use of decorators
		"experimentalDecorators": true
	}
}
