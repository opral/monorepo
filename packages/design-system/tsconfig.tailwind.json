// Tailwind requires commonjs... hence, a separate config file.
{
	"extends": "@inlang/source-code-configs/typescript/tsconfig.base.json",
	"include": ["src/**/*"],
	"exclude": ["src/components", "**/*.test.*"],
	"compilerOptions": {
		"lib": ["ESNext", "DOM"],
		"outDir": "dist",
		"module": "Node16"
	}
}
