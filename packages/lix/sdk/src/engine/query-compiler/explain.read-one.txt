Original SQL:
select * from "state" as "s" where "s"."schema_key" = ? and "s"."entity_id" = ?

Compiled SQL:
select * from "state" as "s" where "s"."schema_key" = ? and "s"."entity_id" = ?

Plan:
  - SCAN internal_state_vtable VIRTUAL TABLE INDEX 0:version_id,schema_key,entity_id
  - LIST SUBQUERY 7
  - SCAN internal_state_vtable VIRTUAL TABLE INDEX 0:schema_key,version_id
  - CREATE BLOOM FILTER
  - CORRELATED SCALAR SUBQUERY 4
  - COMPOUND QUERY
  - LEFT-MOST SUBQUERY
  - SEARCH c USING INDEX sqlite_autoindex_internal_change_1 (id=?)
  - UNION ALL
  - SEARCH t USING INDEX sqlite_autoindex_internal_transaction_state_1 (id=?)
