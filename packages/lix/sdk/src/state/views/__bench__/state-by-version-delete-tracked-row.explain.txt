-- label --
state_by_version delete • tracked row

-- original SQL --
DELETE FROM state_by_version
      WHERE entity_id = ? AND schema_key = ? AND file_id = ? AND version_id = ?

-- rewritten SQL --
DELETE FROM lix_internal_state_vtable AS state_by_version WHERE entity_id = ? AND schema_key = ? AND file_id = ? AND version_id = ?

-- plan --
[
  {
    "id": 3,
    "parent": 0,
    "notused": 176,
    "detail": "SCAN state_by_version VIRTUAL TABLE INDEX 0:entity_id,schema_key,file_id,version_id"
  }
]
