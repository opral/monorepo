-- label --
state_by_version update • tracked row

-- original SQL --
UPDATE state_by_version
      SET snapshot_content = ?, metadata = ?
      WHERE entity_id = ? AND schema_key = ? AND file_id = ? AND version_id = ?

-- rewritten SQL --
UPDATE lix_internal_state_vtable AS state_by_version SET snapshot_content = ?, metadata = ? WHERE entity_id = ? AND schema_key = ? AND file_id = ? AND version_id = ?

-- plan --
[
  {
    "id": 3,
    "parent": 0,
    "notused": 176,
    "detail": "SCAN state_by_version VIRTUAL TABLE INDEX 0:entity_id,schema_key,file_id,version_id"
  }
]
