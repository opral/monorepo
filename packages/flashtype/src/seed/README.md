This folder contains real Markdown files used for seeding a fresh Lix
workspace during development.

Import with Vite's `?raw` to inline the content as strings, then write
to the `file` table via the Lix SDK.
