{
	"$schema": "https://railway.com/railway.schema.json",
	"build": {
		"builder": "RAILPACK",
		"buildCommand": "pnpm install --frozen-lockfile && pnpm --filter @opral/flashtype... build"
	},
	"deploy": {
		"startCommand": "caddy file-server --root dist --listen :$PORT"
	}
}
